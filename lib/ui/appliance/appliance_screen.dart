import 'package:flutter/material.dart';

class ApplianceScreen extends StatelessWidget {
  const ApplianceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Appliance Screen'),
    );
  }
}
