import 'package:flutter/material.dart';

class BloodTypeScreen extends StatelessWidget {
  const BloodTypeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BloodType Screen'),
    );
  }
}
